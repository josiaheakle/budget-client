import * as React from "react"
import { Page } from "../Page"

interface ExpensePageProps {}

const ExpensePage: React.FC<ExpensePageProps> = ({}) => {
	return <Page>expenses</Page>
}

export { ExpensePage }
